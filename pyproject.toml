[project]
name = "multimedia_retrieval"
version = "0.1.0"
requires-python = ">=3.14"

dependencies = [
    "PyOpenGL>=3.1.10",
    "PyOpenGL_accelerate>=3.1.10",
    "matplotlib>=3.11",
    "pandas>=3.0.6"
]

[tool.setuptools]
py-modules = ["main", "obj_viewer"]